-- ! where clause study

-- ? Select rows from a Table or View 

SELECT * FROM bookings b
WHERE  	b.id >100;

-- ? conditions that can be used with when

-- *********************************
--?     =,
--?     != or <>,
--?     >,
--?     <,
--?     <=,
-- ?    >=
-- *********************************